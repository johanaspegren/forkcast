Original prompt: oh, we could perhaps do these changes in the code as well? as a try?

## 2026-08-13

- Started a scoped visual experiment for the Forkcast phone UI: bright casual board-game colors, round/glossy surfaces, hearts/support motifs, and replaceable assets.
- Keep backend/game rules unchanged. Avoid touching unrelated dirty worktree files.
- Added replaceable SVG placeholder assets under `public/assets/forkcast/`, a `src/uiAssets.ts` registry, token/avatar/heart hooks in existing components, and a broad CSS theme pass for the phone UI.
- Verified `npm run build` passes. Browser-checked 390px join screen, simulation lobby, meal selection, and selecting/placing Yakiniku on Monday. No document-level horizontal overflow found.
- Updated the frontend submit flow so `Submit Suggestions` auto-advances from placement through reveal into negotiation when the backend session reaches `REVEAL`; with simulated players, it also auto-simulates their placement after yours.
- Verified the real browser path at 390px: create simulation, start draft, place Yakiniku/Tacos/Salmon, press Submit Suggestions, and land directly on `NEGOTIATION`. Added a small mobile proposal-card stacking fix after visual inspection.
- Added week number display and a client-side `Save Week` button to the completed Forkcast screen. Saved records are stored in `localStorage` under `forkcast.savedWeeks` by ISO week id, ready to replace with backend persistence later.
- Added hallway display week navigation controls. `/display` defaults to the current ISO week and includes Previous Week, Current Week, and Next Week controls that update the displayed week label/date range.
- Verified `/display?mock=1` at tablet size: defaulted to Week 33 · 2026 for August 13, 2026, Next Week changed label to Week 34 · 2026, and Previous Week returned to Week 33.
- Adjusted week defaults for Sunday planning: Sundays default to the coming ISO week and show a small "Planning week" hint; Current Week still jumps back to the actual current calendar week.
- Added backend saved-week persistence via `/api/saved-weeks`, stored in `.forkcast-data/saved-weeks.json`. The phone Save Week button now saves to the server first and caches locally as a fallback.
- Updated `/display` so it can load saved backend dinner plans without a live session id. `/display` loads the default selected week, `/display?week=33` loads that week in the current year, and `/display?week=2026-W33` loads an explicit saved week.
- Fixed the final screen Save Week button initial state: it now only starts as `Saved` when the local cached saved week belongs to the same session id, not merely because another plan exists for the same week.
- Added selectable Realtime Rush mode alongside Classic Draft. Rush starts after Reveal, runs a 60-second realtime heart/freeze phase, has 3-second house-rule override warnings, auto-finalizes the week, and shows awards/voting stats on the final screen. Verified backend smoke path and `npm run build`.
- Polished Realtime Rush interaction after playtest feedback: heart clicks now trigger flying heart bursts and a generated WebAudio "oink", proposal ordering stays stable while leaders get star/color treatment, streak badges show accumulated hearts, and the rush timer/warning banner are fixed to the viewport while scrolling. Verified `npm run build`; browser check still blocked by occupied dev-server port range.
- Added a visible 3-2-1 pre-rush countdown overlay. Backend blocks heart/freeze actions during the countdown and extends the Rush end time so the active phase still lasts 60 seconds after START. Verified countdown behavior with a backend smoke test and `npm run build`.
- Added Cook/Clean controls to Realtime Rush cards using the existing action-card flow, allowed during Rush without turn order and blocked during the countdown. Converted the streak label into an absolute animated popover that only appears during recent heart bursts, so it no longer shifts the Heart/Freeze buttons. Verified Rush chore smoke test and `npm run build`.
- Made Realtime Rush daily leaders more visual with an oversized translucent rank-1 layer plus a leader pill, and made the Heart button larger/icon-first while keeping card layout stable. Verified `npm run build`.
- Compacted Realtime Rush meal cards: Heart, Freeze, Cook, and Clean are now icon-only square controls in one toolbar, freeing room for a larger rank-1 visual. Rush day containers now have stronger colored headers/borders, and meal owner text uses a small "by" separator for readability. Verified `npm run build`.
- Locked Realtime Rush card geometry during voting: fixed proposal rows/action toolbar height, fixed score pill width with tabular numbers, ellipsized meal/owner text, removed tap transforms, and reserved leader outline space so leader changes do not move the Heart button. Verified `npm run build`.
- Fixed leader-card heart animation hiccup: the leader z-index rule no longer overrides absolute positioning for heart bursts/streak popovers, so clicking Heart on the leading meal should not expand the card and hearts should fly as they do on non-leaders. Verified `npm run build`.
- Equalized Realtime Rush meal-card visual height: cards now use a fixed height, leader highlight is an inset ring instead of an outside outline, and the heart/score pill is larger with a wider fixed score column for better visibility. Verified `npm run build`.
- Fixed clipped Rush toolbar icons after equal-height pass: card layout now has a dedicated fixed toolbar row, meal/score content sits in a top row, and volunteer labels are overlaid so they do not push or hide the icon controls. Verified `npm run build`.
- Differentiated Realtime Rush day sections: each weekday now has its own color theme, larger high-contrast day name, stronger header band, and tinted container background. Verified `npm run build`.
